{
  "id": "valid_workflow",
  "name": "Valid Workflow",
  "description": "Checks that a valid generic_agent workflow passes validation.",
  "workflow": {
    "entrypoint": "input_node",
    "nodes": [
      {"id": "input_node", "type": "input", "config": {}, "outputs": ["payload"]},
      {"id": "provider_node", "type": "provider", "config": {"provider_name": "mock"}, "outputs": ["output", "usage"]},
      {"id": "tool_node", "type": "tool", "config": {"tool_name": "mock_echo"}, "outputs": ["result", "status"]},
      {"id": "final_node", "type": "final", "config": {}, "outputs": ["final_output"]}
    ],
    "edges": [
      {"from": "input_node", "to": "provider_node"},
      {"from": "provider_node", "to": "tool_node"},
      {"from": "tool_node", "to": "final_node"}
    ],
    "terminal_nodes": ["final_node"]
  },
  "expected_valid": true,
  "expected_error_codes": []
}
