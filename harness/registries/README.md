# Registries

This directory contains generic registry helpers for Agent Harness Template.

V0.1.8 includes the minimal module registry used to discover module manifests
and resolve agent execution entrypoints. It does not create business modules and
does not implement remote plugin loading.
